<% unless user_signed_in? %>
  <h1>Salus</h1>
  <h3 class="muted">
    All your medical history in one secure place.
  </h3>
  <%= link_to "Start Now!", new_user_registration_path, class: "btn btn-large btn-success" %>
<% else %>
  <%=  render 'index' %>
<% end %>